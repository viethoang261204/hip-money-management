services:
- type: web
  name: hip-money-management
  env: node
  plan: free
  buildCommand: npm install
  startCommand: npm start
  envVars:
  - key: NODE_ENV
    value: production
  - key: DATABASE_URL
    fromDatabase: hip-money-db

databases:
- name: hip-money-db
  plan: free

